# Copia este archivo a ".env" y complétalo si vas a usar el modo REAL.

# "mock" -> no requiere API key, usa el generador local.
# "real" -> llama a la API de Anthropic (requiere ANTHROPIC_API_KEY).
AI_MODE=mock

# Solo necesaria si AI_MODE=real
ANTHROPIC_API_KEY=

PORT=3000
